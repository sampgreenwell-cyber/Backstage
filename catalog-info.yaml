apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage
  description: An example of a Backstage application.
  annotations:
    # Required by @backstage-community/plugin-github-actions - without it
    # the CI/CD tab renders but shows no data. Any other catalog component
    # (including ones scaffolded via examples/express-template) needs the
    # same annotation to get a working CI/CD tab.
    github.com/project-slug: sampgreenwell-cyber/Backstage
spec:
  type: website
  owner: john@example.com
  lifecycle: experimental
